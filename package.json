{
    "name": "ur-jsonpatch",
    "version": "1.1.0",
    "main": "dist/cjs/index.js",
    "module": "dist/esm/index.js",
    "types": "dist/esm/index.d.ts",
    "scripts": {
        "build": "tsc && tsc -p tsconfig.cjs.json && rimraf dist/cjs/**/*.d.ts dist/**/*.spec.*",
        "test": "karma start --single-run --browsers ChromeHeadless karma.config.js",
        "watch": "karma start --auto-watch --browsers ChromeDebugging karma.config.js",
        "clean": "rimraf dist"
    },
    "devDependencies": {
        "rimraf": "^2.6.2",
        "typescript": "^2.7.2",
        "ur-test": "^0.0.1"
    },
    "dependencies": {
        "jiff": "^0.7.3"
    }
}
